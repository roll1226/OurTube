rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    match /ogp/{allPaths=**} {
      allow read: true;
      allow write: false;
    }

    match /signIn/{allPaths==**} {
      allow read: if request.auth != null;
      allow write: false;
    }
  }
}
